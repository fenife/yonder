const config = {
  baseUrl: "/api",
  pyurl: "http://server:6070/api",
  gourl: "http://server:6060/api",
  tokenExpires: 1,  // day
  pageSize: 10,
}

// module.exports = config
export default config
