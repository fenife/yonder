const config = {
  baseUrl: "/api",
  pyurl: "http://server:8010/api",
  gourl: "http://server:8020/api",
  tokenExpires: 1,  // day
  pageSize: 10,
}

// module.exports = config
export default config
